//  Bad practices
var numeroUno = 1;
var second = 1;

// Why doesn't it work?
var if_condition = "no se declara en variable";

// Should equal formula for wild wings
var Buffalowings = wings + buffalo;

// Null variable
var Null = null;

// All variables should equal 1
var uno = 1;
    one = 1;
    wan = 1;
    uma = 1;
    um = 1;
console.log(one);

// It's broken
console.log();

// Console.log para mostrar [area] de un cuadrado
console.log(ladoa * ladob);

// Should equal 4
var four = 2 + 2; //Quitar comillas para evitar concatenacion

// Doesn't work for math problems
var pi = 3.141516; //No es tipo numero, quitar comillas
